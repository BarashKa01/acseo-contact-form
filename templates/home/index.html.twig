{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur le formulaire de contact{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h1>Choisissez une option</h1>
        <a class="button" href="{{ path('contact') }}">Faire une demande sans authentification</a><br>
        <a class="button" href="{{ path('login') }}">Se loguer</a>
</div>
{% endblock %}
